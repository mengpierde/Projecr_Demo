/**
 * 用户信息
 * @type {}
 */
module.exports = {
    getPoint: {
        // 获取点位
        queryPersonData: {
            path: '/getPoint/queryPersonData', // 获取点位
            method: 'POST',
        },
    }
}
